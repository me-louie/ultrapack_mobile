import 'package:flutter/material.dart';

class Backpacks extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: Text('Backpacks')));
  }
}
